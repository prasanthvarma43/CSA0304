#include <stdio.h>
#define SIZE 10

int hash[SIZE] = {0};

void insert(int key) {
    int i = key % SIZE;
    while (hash[i] != 0) i = (i + 1) % SIZE;
    hash[i] = key;
}

void display() {
    for (int i = 0; i < SIZE; i++) printf("%d ", hash[i]);
}

int main() {
    int keys[] = {23, 43, 13, 27};
    for (int i = 0; i < 4; i++) insert(keys[i]);
    display();
}
